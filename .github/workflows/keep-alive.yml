name: Keep API Alive
on:
  schedule:
    - cron: '*/5 * * * *'
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping API health endpoint
        run: curl -s -o /dev/null -w "%{http_code}" https://historias-api-crud-v2.vercel.app/health
